version: '3'
services:
        web:
                image: flask_port_v1
                build: .
                ports:
                        - "4444:8086"
                volumes:
                        - ./instance:/instance
                restart: unless-stopped
